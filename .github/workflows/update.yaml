name: Update infrastructure

on:
  workflow_call:
    secrets:
      repo_bot_private_key:
        description: 'Private key for the repo bot'
        required: true
  workflow_dispatch:

jobs:
  dispatch-infra-deps:
    uses: Portfolio-Solver-Platform/deps-bot/.github/workflows/trigger-run.yaml@ca767769a2c3400e97db24f0b4adfaec9724b874 # main
    with:
      repository: ${{ github.repository_owner }}/infrastructure
    secrets:
      repo_bot_private_key: ${{ secrets.repo_bot_private_key || secrets.REPO_BOT_PRIVATE_KEY }}
