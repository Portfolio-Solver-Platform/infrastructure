
# Google Cloud Deployment

Prerequisites:

- Make sure to be in the Nix environment: `nix develop`

Steps for deployment:

- Log in to Google Cloud: `gcloud auth application-default login`
- Set the project ID: `gcloud config set project PROJECT_ID`
- Run the `init` script
