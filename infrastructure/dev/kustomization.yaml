---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base/

patches:
  - patch: |-
      apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      metadata:
        name: auth-manager
        namespace: keycloak
      spec:
        chart:
          spec:
            valuesFiles:
              - ./helm/values.yaml
              - ./helm/values-dev.yaml
  - patch: |-
      apiVersion: infra.contrib.fluxcd.io/v1alpha2
      kind: Terraform
      metadata:
        name: keycloak-setup
        namespace: flux-system
      spec:
        vars:
          - name: environment
            value: "dev"
          - name: keycloak_url
            value: "http://keycloak-service.keycloak.svc.cluster.local:8080"
  - patch: |-
      apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      metadata:
        name: gateway-routes
        namespace: nginx-gateway
      spec:
        chart:
          spec:
            valuesFiles:
              - ./charts/routes/values.yaml
              - ./charts/routes/values-dev.yaml
